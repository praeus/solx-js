{
  "name": "@solx/surface",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types, errors, and path helpers for the solx SDK. Re-exported under solx/surface.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(no tests in @solx/surface — see @solx/sdk for end-to-end)\" && exit 0",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
